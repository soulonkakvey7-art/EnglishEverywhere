rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global safety net
    match /{document=**} {
      allow read, write: if false;
    }

    match /cached_lessons/{lessonId} {
      allow read: if true;
      allow create, update: if isValidId(lessonId) && isValidCachedLesson(request.resource.data);
    }

    match /dictionary_translations/{wordId} {
      allow read: if true;
      allow create, update: if isValidId(wordId) && isValidDictionaryTranslation(request.resource.data);
    }

    match /dictionary_letters/{letterId} {
      allow read: if true;
      allow create, update: if isValidId(letterId) && isValidDictionaryLetter(request.resource.data);
    }
  }
}

function isValidId(id) {
  return id is string && id.size() <= 200;
}

function isValidCachedLesson(data) {
  return data.key is string
      && data.type is string
      && data.topic is string
      && (data.content is map || data.content is list)
      && data.createdAt is string;
}

function isValidDictionaryTranslation(data) {
  return data.word is string
      && data.khmer is string
      && data.updatedAt is string;
}

function isValidDictionaryLetter(data) {
  return data.translations is map
      && data.updatedAt is string;
}

